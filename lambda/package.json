{
  "name": "lambda",
  "version": "1.0.0",
  "description": "Source of truth for the `birch-api-proxy` Lambda (us-east-1, nodejs24.x,\r handler `index.handler`), which fronts every external API the dashboard uses:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.123.0"
  }
}
